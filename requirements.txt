torch==2.8.0 --index-url https://download.pytorch.org/whl/cu124 # nvidia-smi reports CUDA v12.4
numpy==2.3.3
nvidia-ml-py==13.580.82  # Useful for high level, "during training" GPU utilization measurements
pandas==2.3.3
tabulate==0.9.0
megatron-core
deepspeed
mpi4py  # Required by deepspeed, which also depends on sudo apt-get install openmpi-bin libopenmpi-dev

# Could be useful for plotting results?
matplotlib
pandas
-e . # For development and script execution, do a local + editable install
